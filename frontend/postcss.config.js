// postcss.config.js
module.exports = {
  plugins: {
    // Tailwind’s PostCSS integration lives here now:
    '@tailwindcss/postcss': {},
    // autoprefixer can stay the same
    autoprefixer: {},
  }
}
